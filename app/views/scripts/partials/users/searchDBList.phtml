<?php
$params = unserialize($this->searchString);
if (is_null($params)) {
    $params = array('module' => 'database', 'controller' => 'search', 'action' => 'results');
}
Zend_Debug::dump($params);
$url = $this->url($params, null, true);
Zend_Debug::dump($url);

?>
<div class="row-fluid">
    <?php
    echo $this->gravatar($this->email, array(
        'imgSize' => 40,
        'defaultImg' => $this->serverUrl() . $this->baseUrl()
            . '/assets/gravatar.png',
        'secure' => true),
        array(
            'class' => 'avatar pull-left stelae img-circle',
            'title' => $this->username . '\'s gravatar representation',
            'rating' => 'G',
            'width' => 40,
            'height' => 40)
    );
    ?>    <a href="<?php echo $url; ?>"
             title="Permanent link"><?php echo strip_tags($this->title); ?></a>: <?php echo $this->searchDescription; ?>
    <br/>
    Created by username: <a
        href="/users/named/person/as/<?php echo $this->username; ?>"><?php echo $this->username; ?></a> Search saved for
    reuse: <span class="published timestamp"
                 data="{time:'<?php echo date('W3C', strtotime($this->created)); ?>'}">
<?php echo $this->timeAgoInWords($this->created); ?></span>
</div>
