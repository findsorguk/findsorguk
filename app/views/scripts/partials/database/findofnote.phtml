<?php if (($this->findofnote > 0) && (!is_null($this->reason))): ?>
    <p>
        This is a find of note and has been designated: <?php echo $this->reason; ?>
    </p>
<?php elseif (($this->findofnote > 0) && (is_null($this->reason))): ?>
    <p>
        This has been noted as an interesting find by the recorder.
    </p>
<?php endif; ?>