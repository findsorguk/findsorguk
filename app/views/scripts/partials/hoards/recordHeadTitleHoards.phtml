<?php
$this->headTitle('Hoard ID: ' . $this->hoardID . " - " . $this->broadperiod . " HOARD", $type = 'PREPEND');
$this->metaBase()
    ->setDescription($this->description)
    ->setSubject('archaeological object record')
    ->setKeywords(implode(',', array($this->broadperiod, $this->county)))
    ->generate();
?>