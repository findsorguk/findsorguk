<?php
$this->headTitle('Save this search for future use');
$this->metaBase()->setDescription('Save a search from the database for future use')
    ->setKeywords()
    ->generate();
?>
    <h2 class="lead">
        Save your search for reuse
    </h2>

<?php Zend_Debug::dump($this->params);?>
<?php echo $this->searchParams($this->params);?>

<?php echo $this->form; ?>