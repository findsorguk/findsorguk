<?php
$paginator = $this->results;
$results = array();
foreach($this->events as $result){
	$results[] = $result;
}
$meta = array(
    'currentPage' => $paginator->getCurrentPageNumber(),
    'totalResults' => $paginator->getTotalItemCount(),
    'resultsPerPage' => $paginator->getItemCountPerPage(),
    'totalPages' => ceil($paginator->getTotalItemCount()/$paginator->getItemCountPerPage()));
echo Zend_Json::prettyPrint(Zend_Json::encode( ( array('meta' => $meta, 'archivedEvents' => $results) ) ) ) ;