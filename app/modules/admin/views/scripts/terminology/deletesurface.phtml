<?php
$this->headTitle('Delete a surface treatment from the system');
$this->metaBase()->setDescription($this->title())->generate();
?>
<?php if (!is_null($this->surface)) : ?>
    <h2 class="lead">Delete <?php echo $this->surface->term; ?></h2>
    <p>Are you sure that you want to delete <?php echo $this->surface->term; ?></p>
    <form
        action="<?php echo $this->url(array('module' => 'admin', 'controller' => 'terminology', 'action' => 'deletesurface'), null, true); ?>"
        method="post">
        <div>
            <input type="hidden" name="id" value="<?php echo $this->note->id; ?>"/>
            <input type="submit" name="del" value="Yes"/>
            <input type="submit" name="del" value="No"/>
        </div>
    </form>
<?php else: ?>
    <h2 class="lead">An error has occurred</h2>
    <p>Cannot find the item requested.</p>
<?php endif; ?>